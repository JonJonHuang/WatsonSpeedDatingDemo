pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        echo 'Test'
        echo 'Pulling from GitHub'
      }
    }
    stage('Merge') {
      steps {
        echo 'Merge'
      }
    }
  }
}
